{
  "name": "<%= slugifiedAppName %>",
  "version": "0.0.1",
  "description": "<%= appDescription %>",
  "main": "app/index.js",<% if (repoUrl) { %>
  "repository": "<%= repoUrl %>",<% } %>
  "author": {
    "name": "<%= appAuthor %>"
  },
  "keywords": [
    "<%= appKeywords %>"
  ],
  <% if (isPrivate) { %>
  "private": true
  <% } %>
}
